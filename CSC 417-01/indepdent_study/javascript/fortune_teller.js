const main = (noOfChildren, partner, location, jobTitle ) => {
    return  `You will be a ${jobTitle} in ${location} and married to ${partner} with ${noOfChildren}`
}

console.log(main(50, 'Kaley', 'Africa', 'Dancer'))
